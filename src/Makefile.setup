# Makefile setup for VeloxChem shared library

# C++ compiler & linker

CXX := clang++	

# Compiler flags for clang++ compiler 

CXX_REL_FLG := -std=c++20 -O3 -Xpreprocessor -fopenmp -march=native -Wall
CXX_DEB_FLG := -std=c++20 -O3 -Xpreprocessor -fopenmp -march=native -Wall -g 

# Vector math library, so that the vectorizer replaces the scalar math calls of
# the integral kernels, the exponential above all, with their vector
# counterparts. Apple systems carry them in libsystem_m, glibc in libmvec.

ifeq ($(shell uname -s),Darwin)
VECLIB_FLG := -fveclib=Darwin_libsystem_m
else
VECLIB_FLG := -fveclib=libmvec
endif

CXX_REL_FLG += $(VECLIB_FLG)
CXX_DEB_FLG += $(VECLIB_FLG)

# Generic preprocessor flags

CPP_REL_FLG := 
CPP_DEB_FLG := -DVAR_DEBUG=1 

# disable dynamic lookup

LIB_LIST = #-undefined dynamic_lookup

# Python interface

PYTHON := python3.14

PYTHON_INC += $(shell $(PYTHON) -m pybind11 --includes)
PYTHON_LD  := -L /opt/homebrew/opt/python@3.14/Frameworks/Python.framework/Versions/3.14/lib/ -lpython3.14

CXX_REL_FLG += -fPIC $(PYTHON_INC)
CXX_DEB_FLG += -fPIC $(PYTHON_INC)

# OpenMP interface

OPENMP_INC := -I /opt/homebrew/opt/libomp/include
OPENMP_LD  := -L /opt/homebrew/opt/libomp/lib -lomp

CXX_REL_FLG += $(OPENMP_INC)
CXX_DEB_FLG += $(OPENMP_INC)

# LibXC interface

LIBXC_INC := -I /opt/libxc/include
LIBXC_LD  := -L /opt/libxc/lib -lxc -Wl,-rpath,/opt/libxc/lib

CXX_REL_FLG += $(LIBXC_INC)
CXX_DEB_FLG += $(LIBXC_INC)

# Boost interface

LIBBST_INC := -I /opt/homebrew/opt/boost/include
LIBBST_LD  := -L /opt/homebrew/opt/boost/lib -lboost_math_c99

CXX_REL_FLG += $(LIBBST_INC)
CXX_DEB_FLG += $(LIBBST_INC)

# Eigen interface

EIGEN_INC := -I /Users/rinkevic/Development/Libraries/Eigen

CXX_REL_FLG += $(EIGEN_INC)
CXX_DEB_FLG += $(EIGEN_INC)

# Math library interface
#
# The hardware specific math library provides the dense linear algebra, such as
# the Cholesky factorization used to invert the packed matrices. It is selected
# per platform below: Accelerate on macOS, and MKL or OpenBLAS elsewhere, whose
# link line is set by the person building the code.
#
# Defining VLX_USE_MATHLIB is what selects it. When no math library is set the
# define is absent and the Eigen implementation is compiled in its place, so a
# machine without one needs no further change.

UNAME_S := $(shell uname -s)

ifeq ($(UNAME_S),Darwin)
  MATH_LD  := -framework Accelerate
  MATH_FLG := -DVLX_USE_MATHLIB -DACCELERATE_NEW_LAPACK
endif

CXX_REL_FLG += $(MATH_FLG)
CXX_DEB_FLG += $(MATH_FLG)

# Native shell commands

CP := cp -f
MV := mv -f
RM := rm -f

# Native libraries generation tools

AR      := ar
ARFLAGS := rc
